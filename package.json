{
  "name": "reflexity-ram-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Reflexity — wholesale B2B memory and IT hardware platform",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "test": "npm --prefix frontend test && npm --prefix backend test",
    "build:frontend": "cd frontend && npm run build",
    "verify": "npm test && npm run build:frontend && npm run scan:secrets",
    "seed": "cd backend && node src/scripts/seed.js",
    "install:all": "npm ci && npm --prefix backend ci && npm --prefix frontend ci",
    "scan:secrets": "node scripts/scan-secrets.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "license": "Apache-2.0"
}
